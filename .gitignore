# Local tool/editor settings — not part of the deployed site
.claude/

# OS junk files
.DS_Store
Thumbs.db

# Fotos originales sin optimizar (pesan ~87MB) — las versiones ya
# comprimidas y redimensionadas que usa el sitio viven en
# assets/img/products/. No subir las originales al repo.
Fotos productos/

# Fotos originales de los barberos (una pesa 6.6MB) — las versiones
# recortadas y optimizadas viven en assets/img/team/.
Barberos/

# Fotos originales para la galería/feed (pesan varios MB cada una) —
# las versiones optimizadas viven en assets/img/gallery/.
Fotos para el feed/

# Local environment secrets (never commit real tokens/keys)
.env
.env.local
